{
  "name": "lofoten-ghpages",
  "version": "1.0.0",
  "description": "Fagseminar Lofoten 2016",
  "scripts": {
    "build": "jade --out . --obj src/viewmodel.json --pretty src/index.jade",
    "build:prog": "jade --out . --obj src/viewmodel.json --pretty src/prog.jade",
    "build:stats": "jade --out . --obj src/attendees.json --pretty src/stats.jade",
    "watch:stats": "jade --out . --obj src/attendees.json --pretty --watch src/stats.jade",
    "start": "jade --out . --obj src/viewmodel.json --pretty --watch src/index.jade"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/knowit/lofoten.knowit.no.git"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/knowit/lofoten.knowit.no/issues"
  },
  "homepage": "http://lofoten.knowit.no/",
  "devDependencies": {
    "jade": "^1.11.0"
  }
}
